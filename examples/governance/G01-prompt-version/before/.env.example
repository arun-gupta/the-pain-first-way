# The override nobody committed and nobody can see from git.
# This silently wins over DEFAULT_PROMPT in server.py at runtime.
# Two weeks ago someone "just tweaked the prompt" by editing this line on the box.
SYSTEM_PROMPT="You are a terse support assistant. Answer in one sentence."
